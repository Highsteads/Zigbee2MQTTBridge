[pytest]
# Collect only the test suite. The bundle vendors third-party packages under
# Contents/Packages/, and numpy ships its own conftest.py that imports
# hypothesis — bare `pytest` at the repo root used to abort collection on it.
testpaths = tests
norecursedirs = *.indigoPlugin
